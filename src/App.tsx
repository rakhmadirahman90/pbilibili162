import React, { useEffect, useRef, useState } from 'react';
import { BrowserRouter as Router, Routes, Route, Navigate, useLocation, useNavigate } from 'react-router-dom';
import { supabase } from './supabase';
import popupFallback from './data/konfigurasi_popup.json';
import Navbar from './components/Navbar'; import Hero from './components/Hero'; import News from './components/News'; import About from './components/About'; import Athletes from './components/Players'; import Ranking from './components/Rankings'; import BadmintonQuiz from './components/BadmintonQuiz'; import Gallery from './components/Gallery'; import RegistrationForm from './components/RegistrationForm'; import Contact from './components/Contact'; import PublicKasView from './components/PublicKasView'; import DokumenPenting from './components/DokumenPenting'; import StrukturOrganisasi from './components/StrukturOrganisasi';
import Login from './components/Login'; import Sidebar from './components/Sidebar'; import ManajemenPendaftaran from './ManajemenPendaftaran'; import ManajemenAtlet from './ManajemenAtlet'; import AdminBerita from './components/AdminBerita'; import AdminMatch from './components/AdminMatch'; import AdminRanking from './components/AdminRanking'; import AdminGallery from './components/AdminGallery'; import AdminContact from './components/AdminContact'; import KelolaNavbar from './components/KelolaNavbar'; import ManajemenPoin from './components/ManajemenPoin'; import AuditLogPoin from './components/AuditLogPoin'; import AdminLaporan from './components/AdminLaporan'; import AdminLogs from './components/AdminLogs'; import AdminTampilan from './components/AdminTampilan'; import KelolaHero from './components/KelolaHero'; import AdminPopup from './components/AdminPopup'; import AdminFooter from './components/AdminFooter'; import AdminAbout from './components/AdminAbout'; import AdminStructure from './components/AdminStructure'; import ManajemenDokumen from './components/ManajemenDokumen'; import { KelolaSurat } from './components/KelolaSurat'; import KasManager from './components/KasManager';
import { ArrowLeft, Menu, Volume2, VolumeX, X } from 'lucide-react'; import { AnimatePresence, motion } from 'framer-motion';
const MARS_URL = 'https://missjyvqfehamtpyodjr.supabase.co/storage/v1/object/public/assets/Mars%20US162.mp3';
function ScrollToTop() { const { pathname } = useLocation(); useEffect(() => { window.scrollTo({ top: 0, behavior: 'auto' }); }, [pathname]); return null; }
function ImagePopup() { const [isOpen, setIsOpen] = useState(false); const [promoImages, setPromoImages] = useState<any[]>([]); useEffect(() => { let cancelled = false; const load = async () => { try { const { data, error } = await supabase.from('konfigurasi_popup').select('*').eq('is_active', true).order('created_at', { ascending: false }); if (cancelled) return; const items = !error && data?.length ? data : (popupFallback as any[]).filter(p => p.is_active); if (items.length) { setPromoImages(items); window.setTimeout(() => !cancelled && setIsOpen(true), 1000); } } catch (error) { console.error('Gagal memuat pop-up:', error); } }; load(); return () => { cancelled = true; }; }, []); useEffect(() => { if (!isOpen) return; document.body.classList.add('modal-open'); const onKey = (e: KeyboardEvent) => e.key === 'Escape' && setIsOpen(false); window.addEventListener('keydown', onKey); return () => { document.body.classList.remove('modal-open'); window.removeEventListener('keydown', onKey); }; }, [isOpen]); if (!isOpen || !promoImages.length) return null; const current = promoImages[0]; return <AnimatePresence mode="wait"><div className="fixed inset-0 z-[999999] flex items-center justify-center p-3 sm:p-6 bg-slate-950/90 backdrop-blur-md overflow-y-auto" role="dialog" aria-modal="true" aria-label="Promosi PB Bilibili 162"><button aria-label="Tutup promosi" className="absolute inset-0" onClick={() => setIsOpen(false)} /><motion.div initial={{ opacity: 0, scale: .96 }} animate={{ opacity: 1, scale: 1 }} exit={{ opacity: 0, scale: .96 }} className="relative z-10 w-full max-w-[420px] max-h-[min(90dvh,760px)] bg-white rounded-[2rem] shadow-2xl flex flex-col overflow-hidden" onClick={e => e.stopPropagation()}><button onClick={() => setIsOpen(false)} aria-label="Tutup promosi" className="absolute top-3 right-3 z-20 p-3 bg-white/95 hover:bg-rose-500 hover:text-white text-slate-900 rounded-full shadow-lg"><X size={18} /></button><div className="flex-1 overflow-y-auto overscroll-contain hide-scrollbar"><img src={current.url_gambar} className="w-full h-auto object-cover" alt={current.judul || 'Promosi'} /><div className="p-6 sm:p-8"><h3 className="text-xl font-black uppercase mb-4 break-words">{current.judul}</h3><p className="text-slate-600 text-sm leading-relaxed mb-6 break-words">{current.deskripsi}</p><button onClick={() => setIsOpen(false)} className="w-full py-4 bg-blue-600 hover:bg-blue-700 text-white rounded-xl font-bold uppercase tracking-widest text-[10px]">Tutup</button></div></div></motion.div></div></AnimatePresence>; }
function PublicShell({ children }: { children: React.ReactNode }) { const navigate = useNavigate(); const [isMarsPlaying, setIsMarsPlaying] = useState(false); const audioRef = useRef<HTMLAudioElement | null>(null); const handleNavigate = (path: string, tab?: string) => { if (tab && (path === 'tentang-kami' || path === 'about')) return navigate(`/tentang-kami/${encodeURIComponent(tab)}`); if (tab && (path === 'atlet' || path === 'players')) return navigate(`/atlet/${encodeURIComponent(tab.toLowerCase())}`); const routes: Record<string, string> = { home: '/', about: '/tentang-kami', 'tentang-kami': '/tentang-kami', peringkat: '/peringkat', rankings: '/peringkat', quiz: '/quiz', galeri: '/galeri', gallery: '/galeri', register: '/pendaftaran', pendaftaran: '/pendaftaran', contact: '/kontak', kontak: '/kontak', kas: '/kas', 'dokumen-penting': '/dokumen-penting', struktur: '/struktur' }; navigate(routes[path] || `/${path.replace(/^\//, '')}`); }; const toggleMars = async () => { if (!audioRef.current) return; try { if (isMarsPlaying) { audioRef.current.pause(); setIsMarsPlaying(false); } else { await audioRef.current.play(); setIsMarsPlaying(true); } } catch { setIsMarsPlaying(false); } }; return <div className="min-h-screen w-full overflow-x-clip bg-[#0b0e14] text-white"><Navbar onNavigate={handleNavigate} /><audio ref={audioRef} src={MARS_URL} loop preload="none" /><div className="fixed bottom-4 right-4 sm:bottom-6 sm:right-6 z-[9998] pointer-events-none"><button onClick={toggleMars} aria-label={isMarsPlaying ? 'Matikan Mars PB 162' : 'Putar Mars PB 162'} className="pointer-events-auto w-12 h-12 sm:w-14 sm:h-14 bg-blue-600 hover:bg-blue-500 text-white rounded-full shadow-2xl flex items-center justify-center border border-white/20">{isMarsPlaying ? <Volume2 size={20} /> : <VolumeX size={20} className="text-white/60" />}</button></div><ImagePopup />{children}<footer className="w-full py-8 px-4 text-center text-slate-500 text-sm border-t border-white/5 bg-[#0b0e14]"><p>© 2026 PB BILIBILI 162</p></footer></div>; }
function PublicPage({ children }: { children: React.ReactNode }) { const navigate = useNavigate(); return <PublicShell><main className="min-h-[100dvh] w-full pt-20 pb-24"><div className="w-full max-w-7xl mx-auto px-0 sm:px-4 lg:px-8">{children}</div></main><button onClick={() => navigate('/')} className="fixed bottom-4 left-1/2 -translate-x-1/2 z-[9990] px-5 sm:px-8 py-3 sm:py-4 bg-blue-600 hover:bg-blue-500 text-white rounded-full font-black text-[10px] tracking-[.18em] uppercase flex items-center gap-2 shadow-xl whitespace-nowrap"><ArrowLeft size={15} /> Kembali ke Beranda</button></PublicShell>; }
function HomePage() { return <PublicShell><main><Hero /><section id="berita-section"><News /></section></main></PublicShell>; }
function AboutWrapper() { const location = useLocation(); const tab = location.pathname.split('/').filter(Boolean)[1] || 'sejarah'; const [activeTab, setActiveTab] = useState(tab); useEffect(() => setActiveTab(tab), [tab]); return <About activeTab={activeTab} onTabChange={setActiveTab} />; }
function AthletesWrapper() { const location = useLocation(); const filter = location.pathname.split('/').filter(Boolean)[1] || 'all'; return <Athletes initialFilter={filter.toLowerCase()} />; }
function AdminLayout({ session }: { session: any }) { const [isSidebarOpen, setIsSidebarOpen] = useState(false); return <div className="flex min-h-[100dvh] w-full bg-[#050505] overflow-hidden"><aside className={`fixed inset-y-0 left-0 z-[101] h-[100dvh] w-72 transition-transform duration-300 md:relative md:translate-x-0 ${isSidebarOpen ? 'translate-x-0' : '-translate-x-full'}`}><Sidebar email={session.user.email} isOpen={isSidebarOpen} onClose={() => setIsSidebarOpen(false)} /></aside>{isSidebarOpen && <button aria-label="Tutup menu admin" onClick={() => setIsSidebarOpen(false)} className="fixed inset-0 z-[100] bg-black/60 md:hidden" />}<main className="flex min-w-0 flex-1 flex-col min-h-[100dvh]"><div className="md:hidden sticky top-0 z-[90] flex h-16 items-center justify-between bg-[#0F172A] px-4 border-b border-white/5"><button aria-label="Buka menu admin" onClick={() => setIsSidebarOpen(true)} className="p-2 text-white"><Menu size={22} /></button><div className="text-white font-black italic tracking-tighter text-sm uppercase">Admin Console</div><div className="w-8" /></div><div className="flex-1 overflow-y-auto bg-[#050505] custom-scrollbar"><Routes><Route path="dashboard" element={<ManajemenPendaftaran />} /><Route path="atlet" element={<ManajemenAtlet />} /><Route path="surat" element={<KelolaSurat />} /><Route path="kas" element={<KasManager />} /><Route path="dokumen" element={<ManajemenDokumen />} /><Route path="poin" element={<ManajemenPoin />} /><Route path="audit-poin" element={<AuditLogPoin />} /><Route path="skor" element={<AdminMatch />} /><Route path="berita" element={<AdminBerita />} /><Route path="ranking" element={<AdminRanking />} /><Route path="galeri" element={<AdminGallery />} /><Route path="kontak" element={<AdminContact />} /><Route path="navbar" element={<KelolaNavbar />} /><Route path="laporan" element={<AdminLaporan />} /><Route path="logs" element={<AdminLogs />} /><Route path="tampilan" element={<AdminTampilan />} /><Route path="hero" element={<KelolaHero />} /><Route path="popup" element={<AdminPopup />} /><Route path="footer" element={<AdminFooter />} /><Route path="about" element={<AdminAbout />} /><Route path="struktur" element={<AdminStructure />} /><Route path="*" element={<Navigate to="dashboard" replace />} /></Routes></div></main></div>; }
function AppRoutes({ session }: { session: any }) { return <><ScrollToTop /><Routes><Route path="/" element={<HomePage />} /><Route path="/tentang-kami/:tab?" element={<PublicPage><AboutWrapper /></PublicPage>} /><Route path="/atlet/:filter?" element={<PublicPage><AthletesWrapper /></PublicPage>} /><Route path="/peringkat" element={<PublicPage><Ranking /></PublicPage>} /><Route path="/quiz" element={<PublicPage><BadmintonQuiz /></PublicPage>} /><Route path="/galeri" element={<PublicPage><Gallery /></PublicPage>} /><Route path="/pendaftaran" element={<PublicPage><RegistrationForm /></PublicPage>} /><Route path="/kontak" element={<PublicPage><Contact /></PublicPage>} /><Route path="/kas" element={<PublicPage><PublicKasView /></PublicPage>} /><Route path="/dokumen-penting" element={<PublicPage><DokumenPenting /></PublicPage>} /><Route path="/struktur" element={<PublicPage><StrukturOrganisasi /></PublicPage>} /><Route path="/login" element={!session ? <Login /> : <Navigate to="/admin/dashboard" replace />} /><Route path="/admin/*" element={session ? <AdminLayout session={session} /> : <Navigate to="/login" replace />} /><Route path="*" element={<Navigate to="/" replace />} /></Routes></>; }
export default function App() { const [session, setSession] = useState<any>(null); const [loading, setLoading] = useState(true); useEffect(() => { supabase.auth.getSession().then(({ data: { session } }) => { setSession(session); setLoading(false); }); const { data: { subscription } } = supabase.auth.onAuthStateChange((_event, nextSession) => setSession(nextSession)); return () => subscription.unsubscribe(); }, []); if (loading) return <div className="min-h-[100dvh] flex items-center justify-center bg-[#0b0e14]"><div className="w-12 h-12 border-4 border-blue-600 border-t-transparent rounded-full animate-spin" aria-label="Memuat aplikasi" /></div>; return <Router><AppRoutes session={session} /><style>{`.custom-scrollbar::-webkit-scrollbar{width:6px}.custom-scrollbar::-webkit-scrollbar-track{background:#050505}.custom-scrollbar::-webkit-scrollbar-thumb{background:#1e293b;border-radius:10px}`}</style></Router>; }
